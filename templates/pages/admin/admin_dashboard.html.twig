{% extends 'base.html.twig' %}
{% block title %}Kraken - Bureau Admin
{% endblock %}
{% block body %}

	{% if is_granted('ROLE_ADMIN') %}

		<div class="count text-center">
		<a href="{{ path('velo') }}" class="btn btn-custom  mt-5" role="button" style="width: 16rem; height: 2.75rem;">
			Vélos admin
		</a>
        <div/>
<img src="{{ asset('img/Group4.png') }}" alt="Logo du magasin Kraken" class="img-fluid mt-5 " style="width: 15rem">
        <div class="count text-center">
		<a href="{{ path('admin_category.index') }}" class="btn btn-custom  mt-5" role="button" style="width: 16rem; height: 2.75rem;">
			Catégories admin
		</a>
		<div/>
	{% endif %}


{% endblock %}
