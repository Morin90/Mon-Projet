<nav class="navbar navbar-expand-lg" data-bs-theme="dark" style="background-color: var(--bg-blue);">
    <div class="container-fluid">
        <a href="{{ path('home.index') }}">
            <img class="imageHeader" src="{{ asset('img/Group4.png') }}" alt="Logo du magasin Kraken">
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav me-5">
                <li class="nav-item">
                    <a class="nav-link active text-dark" href="{{ path('home.index') }}">
                        Accueil
                        <span class="visually-hidden">(current)</span>
                    </a>
                </li>

                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item nav-separator">
                        <a class="nav-link text-dark" href="{{ path('admin_dashboard') }}">
                            Bureau Admin
                            <span class="visually-hidden">(current)</span>
                        </a>
                    </li>
                {% endif %}

                {% if app.user %}
                    <li class="nav-item nav-separator">
                        <a class="nav-link text-dark" href="{{ path('user.edit', {'id': app.user.id}) }}">
                            Profil
                            <span class="visually-hidden">(current)</span>
                        </a>
                    </li>
                {% endif %}

                <li class="nav-item nav-separator">
                    <a class="nav-link text-dark" href="{{ path('category.show') }}">
                        Catégories
                        <span class="visually-hidden">(current)</span>
                    </a>
                </li>
                <li class="nav-item nav-separator">
                    <a class="nav-link text-dark" href="{{ path('velo.showall') }}">
                        Vélos
                        <span class="visually-hidden">(current)</span>
                    </a>
                </li>
            </ul>
        </div>

        <form class="searchbar me-5" action="{{ path('velo.showall') }}">
            <input type="search" name="q" placeholder="Nom du vélo" class="btn btn-light btn-outline-dark border-2 w-20">
            <button type="submit" class="btn1" role="button" style="background-color: black; color: var(--bg-blue);">Rechercher</button>
        </form>

        {% if app.user %}
            <div class="nav-separator">
                <a class="nav-link fs-5 text-dark ms-3" href="{{ path('security.logout') }}">
                    Déconnexion
                </a>
            </div>
        {% else %}
            <div class="d-flex nav-separator ms-3">
                <a class="nav-link fs-5 text-dark" href="{{ path('security.login') }}">
                    Connexion
                </a>
                <a class="nav-link fs-5 text-dark ms-2" href="{{ path('security.registration') }}">
                    Inscription
                </a>
            </div>
        {% endif %}
    </div>
</nav>
{# Détail

    visually-hidden : Cette classe (proposée par Bootstrap) cache le contenu visuellement, mais elle reste accessible aux lecteurs d'écran. Elle est souvent utilisée pour améliorer l’accessibilité d’une page sans encombrer l’interface visuelle.
    (current) : Ce texte signifie généralement que cet élément de menu est l'élément "actif" ou "en cours". Pour les utilisateurs de lecteurs d'écran, cela indique que cette page est actuellement ouverte.

Utilisation pratique

Dans une barre de navigation, par exemple, cette balise permet d'indiquer clairement quelle page est en cours de consultation pour les utilisateurs non-voyants, tout en restant invisible pour les utilisateurs voyants. #}