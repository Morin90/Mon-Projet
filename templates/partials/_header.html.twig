<nav class="navbar navbar-expand-lg " data-bs-theme="dark" style="background-color: var(--bg-blue);">
	<div class="container-fluid">
		<a href="{{path('home.index')}}"><img class="imageHeader" src="{{asset('img/Group4.png')}}" alt="Logo du magasin Kraken"></img>
	</a>
	<button class="navbar-toggler " type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>
	<div class="collapse navbar-collapse " id="navbarColor01">
		<ul class="navbar-nav me-5 ">
			<li class="nav-item">
				<a class="nav-link active text-dark" href="{{path('home.index')}}">
					Accueil
					<span class="visually-hidden">(current)</span>
				</a>
			</li>
			{% if is_granted('ROLE_ADMIN') %}

				<li class="nav-item">
					<a class="nav-link  text-dark" href="{{ path('velo') }}">
						Vélos admin
					</a>
				</li>
				<li class="nav-item">
					<a class="nav-link  text-dark" href="{{ path('admin_category.index') }}">
						Catégories admin
					</a>
				</li>
			{% endif %}
			{% if app.user  %}

				<li class="nav-item">
					<a class="nav-link  text-dark " href="{{ path('user.edit', {'id': app.user.id}) }}">
						Profil
					</a>
				</li>
			{% endif %}
			<li class="nav-item">
				<a class="nav-link  text-dark" href="{{ path('category.show') }}">
					Catégories
				</a>
			</li>
			<li class="nav-item">
				<a class="nav-link  text-dark" href="{{ path('velo.showall') }}">
					Vélos
				</a>
			</li>
		</ul>

	</div>
	<form class="searchbar me-5 " action="{{ path('velo.showall') }}">
		<input type="search" name="q" placeholder="Nom du vélo" class="btn btn-light w-20">
		<button type="submit" class="btn1 btn-light w-20">Submit
		</button>
	</form>

	{% if app.user  %}
		<div>

			<a class="nav-link  text-dark ms-3 " href="{{ path('security.logout') }}">
				Déconnexion
			</a>
		</div>
	{% else %}
		<div>
			<a class="nav-link  text-dark " href="{{ path('security.login') }}">
				Connexion
			</a>
			<a class="nav-link  text-dark " href="{{ path('security.registration') }}">
			Inscription
			</a>
		</div>
	{% endif %}
</div>
</nav>
